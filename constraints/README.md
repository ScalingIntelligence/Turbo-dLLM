# Constraints

- `gpu-cu128.txt` pins dependencies for official CUDA 12.8 bundles.
- `deepep-source.txt` pins the tested DeepEP source revision.

These files are for GPU artifact builds, not `pip install turbo-dllm`.
